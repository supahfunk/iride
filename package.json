{
  "name": "drag-infinity-grid",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "localhost & parcel app.js & open http://localhost:8080",
    "build": "parcel build app.js --out-dir dist"
  },
  "dependencies": {
    "gsap": "^3.2.1",
    "ogl": "^0.0.40",
    "sass": "^1.25.0"
  },
  "devDependencies": {
    "cssnano": "^4.1.10"
  }
}
